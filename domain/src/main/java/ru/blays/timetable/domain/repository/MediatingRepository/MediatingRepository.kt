package ru.blays.timetable.domain.repository.MediatingRepository

class MediatingRepository {
/*


    // appBar sate
    var currentScreen = ""
    var currentGroupCode = ""

    // theme sate
    var isDarkMode = false
    var isMonetTheme = false

    // appBar CallBack
    var appBarStateCallBack : (String, String) -> Unit = {_, _ ->}
    fun appBarStateCall() { appBarStateCallBack(currentScreen, currentGroupCode) }

    // theme CallBack
    var themeChangeCallBack: (Boolean) -> Unit = {_ ->}
    fun themeChangeCall() { themeChangeCallBack(isDarkMode) }

    //monet CallBack
    var monetChangeCallBack: (Boolean) -> Unit = {_ ->}
    fun monetChangeCall() { monetChangeCallBack(isMonetTheme) }*/
}